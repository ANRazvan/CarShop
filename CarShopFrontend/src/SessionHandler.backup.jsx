import React from 'react';

/**
 * Empty SessionHandler replacement component
 * This version does nothing - it's been disabled to prevent 
 * the constant session expiration overlays.
 */
const SessionHandler = () => {
  // This component is intentionally disabled
  // If you want to re-enable session monitoring, replace this file with 
  // the original implementation from the backup
  return null;
};

export default SessionHandler;
